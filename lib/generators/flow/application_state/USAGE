Description:
    Create the ApplicationState and test.

Example:
    `rails generate flow:application_state`

    Generates:
      State: app/states/application_state.rb
       Test: spec/states/application_state_spec.rb
